// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  firebase: {
    apiKey: 'AIzaSyCllW-d4buFxazvua0Ku4UYNxvT1oV9xXE',
    authDomain: 'my-courses-site.firebaseapp.com',
    databaseURL: 'https://my-courses-site.firebaseio.com',
    projectId: 'my-courses-site',
    storageBucket: 'my-courses-site.appspot.com',
    messagingSenderId: '84561363160',
    appId: '1:84561363160:web:91148cdbbd6e25509057ad'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
